<li>
	{{name}}

	<button class="change-eat" data-id="{{id}}" data-neweat="{{eat}}" id="changed-eat">
		{{#if eat}}Devour it!{{else}}testing{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete!</button>
</li>
